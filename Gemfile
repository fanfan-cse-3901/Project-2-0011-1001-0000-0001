source 'https://rubygems.org'

# install dependencies by `bundle install`
# Disabled for further actions
# gem 'fxruby', '~> 1.6', '>= 1.6.33'

# run following command before install
# Debian: sudo apt-get install libmagickwand-dev
# CentOS: sudo dnf install gcc ImageMagick-devel make which
# macOS: brew install pkg-config imagemagick
gem 'rmagick', '~> 4.1', '>= 4.1.2'